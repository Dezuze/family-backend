services:
  traefik:
    image: traefik:v3.6.12
    container_name: traefik
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "./traefik/traefik.yml:/etc/traefik/traefik.yml:ro"
      - "./traefik/letsencrypt:/letsencrypt"
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.middlewares.secure-headers.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.secure-headers.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.secure-headers.headers.stsPreload=true"
      - "traefik.http.middlewares.secure-headers.headers.browserXssFilter=true"
      - "traefik.http.middlewares.secure-headers.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.secure-headers.headers.frameDeny=true"
      - "traefik.http.middlewares.secure-headers.headers.referrerPolicy=same-origin"
      - "traefik.http.middlewares.compress.compress=true"
      - "traefik.http.middlewares.backend-ratelimit.ratelimit.average=200"
      - "traefik.http.middlewares.backend-ratelimit.ratelimit.burst=100"
    restart: always

  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ~/.docker/config.json:/config.json:ro
    environment:
      - WATCHTOWER_POLL_INTERVAL=15
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_LABEL_ENABLE=true
      - DOCKER_API_VERSION=1.41
    restart: always

  db:
    image: postgres:16-alpine
    shm_size: 128mb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    restart: always

  backend:
    image: ghcr.io/${OWNER_LC:-dezuze}/family-backend:latest
    env_file: ../../.env
    environment:
      DB_ENGINE: postgresql
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_HOST: db
      DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1,${DOMAIN},www.${DOMAIN},api.${DOMAIN},www.api.${DOMAIN},backend"
      CORS_ALLOWED_ORIGINS: "https://${DOMAIN},https://www.${DOMAIN},https://api.${DOMAIN},https://www.api.${DOMAIN}"
      CSRF_TRUSTED_ORIGINS: "https://${DOMAIN},https://www.${DOMAIN},https://api.${DOMAIN},https://www.api.${DOMAIN}"
      DJANGO_COOKIE_DOMAIN: ".${DOMAIN}"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      update_config:
        order: start-first
        failure_action: rollback
        delay: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=Host(`api.${DOMAIN}`)"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls.certresolver=myresolver"
      - "traefik.http.routers.backend.middlewares=secure-headers@docker,compress@docker,backend-ratelimit@docker"
      - "traefik.docker.network=app_web"
      - "traefik.http.services.backend.loadbalancer.server.port=8000"
      - "com.centurylinklabs.watchtower.enable=true"
    volumes:
      - ../../Backend/media:/app/media
      - static_volume:/app/staticfiles
    networks:
      - web
      - internal
    depends_on:
      db:
        condition: service_healthy
    restart: always

  frontend:
    image: ghcr.io/${OWNER_LC:-dezuze}/family-frontend:latest
    env_file: ../../.env
    environment:
      API_BASE: https://api.${DOMAIN}
      NUXT_PUBLIC_API_BASE: https://api.${DOMAIN}
    healthcheck:
      test: ["CMD-SHELL", "node -e 'require(\"http\").get(\"http://localhost:3000/\", (res) => { if (res.statusCode === 200) process.exit(0); else process.exit(1); }).on(\"error\", () => process.exit(1))'"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      update_config:
        order: start-first
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`${DOMAIN}`) || Host(`www.${DOMAIN}`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls.certresolver=myresolver"
      - "traefik.http.routers.frontend.middlewares=secure-headers@docker,compress@docker"
      - "traefik.docker.network=app_web"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"
      - "com.centurylinklabs.watchtower.enable=true"
    networks:
      - web
    restart: always

networks:
  web:
  internal:
    internal: true

volumes:
  postgres_data:
  static_volume: